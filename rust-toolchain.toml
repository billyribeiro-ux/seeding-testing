# Curriculum targets Rust 1.96.0 (stable as of June 2026). sqlx 0.9 requires >= 1.94.
# Pin a specific version once the team is ready to lock it down (e.g. `channel = "1.96.0"`).
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy", "rust-analyzer", "rust-src"]
profile = "minimal"
