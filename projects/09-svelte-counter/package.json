{
  "name": "svelte-counter",
  "private": true,
  "version": "0.1.0",
  "description": "Phase 9 warm-up — Svelte 5 runes intro. No DB, no SSR, no framework features beyond $state / $derived / $effect / $props / $bindable.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@testing-library/svelte": "^5.2.0",
    "@vitest/ui": "^2.1.0",
    "jsdom": "^25.0.0",
    "svelte": "^5.55.0",
    "svelte-check": "^4.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
